package com.tructt.caro.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Background ───
val DarkBackground = Color(0xFF0F0F23)   // Deep OLED dark
val DarkSurface    = Color(0xFF1A1A35)   // Card / button surface
val BgCell         = Color(0xFF1E1E42)   // Empty cell background
val BorderSubtle   = Color(0xFF2A2A50)   // Subtle borders
val CellBorder     = Color(0xFF2D2D5E)   // Cell border (legacy compat)

// ─── Player X — Blue ───
val BlueAccent  = Color(0xFF58A6FF)
val BlueCellBg  = Color(0xFF152535)

// ─── Player O — Coral ───
val CoralAccent  = Color(0xFFF43F5E)
val CoralCellBg  = Color(0xFF2A1525)

// ─── Accent Purple (CTA) ───
val AccentPurple     = Color(0xFF7C3AED)
val AccentPurpleEnd  = Color(0xFFA855F7)  // Gradient end
val AccentPurpleGlow = Color(0x407C3AED)  // Shadow glow (25% alpha)

// ─── Gold (Victory) ───
val GoldAccent = Color(0xFFFFD700)

// ─── Text ───
val TextPrimary   = Color(0xFFFFFFFF)     // White
val TextSecondary = Color(0xFF9CA3AF)     // Gray-400
val TextMuted     = Color(0xFF6B7280)     // Gray-500

// ─── UI Elements ───
val ButtonBg     = Color(0xFF1A1A35)
val ButtonBorder = Color(0xFF2A2A50)
val DividerColor = Color(0xFF2A2A50)
