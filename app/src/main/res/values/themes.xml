<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Caro.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0F172A</item>
        <!-- Use the vector drawable with safe-zone padding, not the square mipmap -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@android:style/Theme.Material.NoActionBar</item>
    </style>
</resources>
